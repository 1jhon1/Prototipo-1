<% include partes/_header %>

<div class="container mt-5">
  <div class="row">
    <div class="col-md-7">
      <table class="table table-bordered table-hover">
        <thead>
          <tr>
            <td>#</td>
            <td>Nombre del Producto</td>
            <td> Precio</td>
            <td>Imagen</td>
            <td>Acción</td>
          </tr>
        </thead>
        <tbody>
          <% if (data) { %>
            <% for(var i = 0; i < data.length; i++) { %>
              <tr>
                
                <td><%= (i + 1) %></td>
                <td><%= data[i].Nombre_del_producto %></td>
                <td><%= data[i].Precio %></td>
        
                <td><img src="<%= data[i].Imagen %>"></td>
              
                <td>
              
                  <a href="/update/<%= data[i].id %>" class="btn btn-info" >  Editar</a> <style>  .btn-info{ width:115px;} </style>
                  <a href="/delete/<%= data[i].id %>" class="btn btn-danger">Eliminar</a>
                </td>
              </tr>
            <% } %>
          <% } %>
        </tbody>
      </table>
    </div>
    <div class="col-md-5">
      <div class="card-body">
        <form action="/add" enctype="multipart/form-data" method="POST" > 
          <div class="form-group">
            <input type="text" name="Nombre_del_producto" placeholder="Nombre del producto" class="form-control" autofocus>
          </div>
          <div class="form-group">
            <input type="text" name="Precio" placeholder="Precio" class="form-control">
          </div>
          <div class="form-group">
            <div class="col-sm-9"><input type="file" name="Imagen" class="form-control"> 
              </div>
          </div>
         
          

          <button type="submit" class="btn btn-success">
            Guardar Producto
          </button>
        </form>
      </div>
    </div>
  </div>
</div>

<% include partes/_footer %>
